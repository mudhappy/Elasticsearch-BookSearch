<p id="notice"><%= notice %></p>

<h1>Books</h1>

<div class="row">
  <div class="search-content col-12">
    <div class="row">
      <%= render 'search_form' %>
    </div>
    <div class="row">
      <%= render partial: 'book', collection: @books %>
    </div>
    <%= paginate @books %>
  </div>
</div>
